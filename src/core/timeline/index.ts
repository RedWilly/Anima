/**
 * Timeline module for scheduling and controlling animation playback.
 */
export { Timeline } from './Timeline';
export type {
    ScheduledAnimation,
    TimelineConfig,
    ResolvedScheduledAnimation,
} from './types';
